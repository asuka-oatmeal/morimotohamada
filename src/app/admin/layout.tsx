export const metadata = {
  title: "管理画面 | 暮らしの法律ガイド",
  robots: { index: false, follow: false },
};

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
